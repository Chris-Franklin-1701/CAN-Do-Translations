<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Registry</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="./public/css/style.css">

</head>

<body>
    <nav class="navbar p-3 mb-2 bg-primary text-white">
        <div id="loginTitle">
            <h1 style="font-family: fantasy; font-size: 50px;">CAN Do Translator</h1>
        </div>

        <ul class="list-inline">
            <li class="list-inline-item"><a class="text-light" href="/">Home</a></li>
            <li class="list-inline-item"><a class="text-light" href="/dashboard">Dashboard</a></li>
            {{#unless logged_in}}
            <li class="list-inline-item"><a class="text-light" href="/login">Login</a></li>
            {{/unless}}
            {{#if logged_in}}
            <li class="list-inline-item"><a class="text-light" href="#" id="logout">Logout</a></li>
            {{/if}}
        </ul>
    </nav>

    <main class="container">
        {{{ body }}}
    </main>

    <footer>
    </footer>

    <!-- Only include the logout script if the user is logged in -->
    {{#if logged_in}}
    <script src="/public/jslogout.js"></script>
    {{/if}}
</body>

</html>